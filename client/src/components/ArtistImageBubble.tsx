import React from "react";

import styles from "../styles/artistimagebubble.module.css";

const ArtistImageBubble: React.FC = () => {
    return (
        <div className={styles["artist-image-bubble"]}></div>
    );
}

export default ArtistImageBubble;